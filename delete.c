#include <stdio.h>
#include <stdlib.h>
#include "mylib.h"

void delete(void) {
	char schoolnumber[20];
	// input the schoolnumber;

	struct tmpNode = start;
	if(strcmp(start->schoolnumber, schoolnumber)) {

	}
	while(tmpNode->next != null) {
		tmpNode = tmpNode->next;
		if(strcmp(tmpNode->schoolnumber, schoolnumber)) {

		}
	}
}
